import { PageShell } from "../PageShell";

export function LoginPage() {
  return (
    <PageShell
      title="Login"
      description="Authentication placeholder for the approved route shell."
    />
  );
}
