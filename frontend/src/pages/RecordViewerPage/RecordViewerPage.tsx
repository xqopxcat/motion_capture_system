import { PageShell } from "../PageShell";

export function RecordViewerPage() {
  return (
    <PageShell
      title="Record Viewer"
      description="Viewer route placeholder without video playback or visualization logic."
    />
  );
}
