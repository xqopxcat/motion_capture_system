import { PageShell } from "../PageShell";

export function ComparePage() {
  return (
    <PageShell
      title="Compare"
      description="Compare route placeholder without sync or playback logic."
    />
  );
}
