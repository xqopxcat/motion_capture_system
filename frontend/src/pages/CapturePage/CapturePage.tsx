import { PageShell } from "../PageShell";

export function CapturePage() {
  return (
    <PageShell
      title="Capture"
      description="Capture route placeholder without camera, recording, or pose logic."
    />
  );
}
