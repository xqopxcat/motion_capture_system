# Task 81 — Toggle and Selection Policy

Skeleton defaults on; Angles defaults off. Both native buttons expose `aria-pressed`, visible On/Off labels and independent state. Route-local React state survives camera flip, recording start/stop and Live-to-Review transitions, but is not persisted across sessions or to backend artifacts.

The immutable `capture-angle-integration.v1` set is left knee then right knee. The four-limb candidate was reduced to two knees because the existing mobile portrait stage and bounded collision layout can become dense with four simultaneous labels. Task 81 adds no selection UI.
